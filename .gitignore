# Android / Gradle
*.iml
.gradle/
/local.properties
.idea/
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
*.apk
*.aab

# Keystores — never commit signing keys
*.jks
*.keystore

# Firebase config — contains project identifiers; add your own, don't commit a shared one
app/google-services.json

# Node / Cloud Functions
functions/node_modules/
functions/lib/
firebase-debug.log
firebase-debug.*.log
.firebase/

# Admin dashboard (React/Vite)
admin-dashboard/node_modules/
admin-dashboard/dist/
